const buttons=document.querySelectorAll('.filters button');const cards=document.querySelectorAll('.project');buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const tag=btn.dataset.filter;cards.forEach(card=>card.style.display=(tag==='all'||card.dataset.tags.includes(tag))?'block':'none')}));
const menu=document.querySelector('.menu');menu.addEventListener('click',()=>{const nav=document.querySelector('nav');const open=getComputedStyle(nav).display==='flex';nav.style.cssText=open?'':'display:flex;position:absolute;right:6vw;top:62px;flex-direction:column;align-items:flex-end;background:#18181a;padding:20px;border:1px solid rgba(255,255,255,.15)';menu.textContent=open?'Меню':'Закрыть'});
window.addEventListener('load',()=>setTimeout(()=>document.getElementById('loader')?.classList.add('hidden'),500));const glow=document.querySelector('.cursor-glow'),dot=document.querySelector('.cursor-dot');window.addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px';dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px'}});